// El operador terminario, tambien conocido como operador condicional.
// es un operador  que toma 3 operandos y se utiliza  para evaluar una expresion booleana

// condicion ? expresion1 : expresion2

let edad = 18;
let mensaje = (edad >= 18) ? 'Eres mayor de edad' : 'Eres menor de edad';
console.log(mensaje);